numpy>=1.22
scipy>=1.9
pandas>=1.5
scikit-learn>=1.1
matplotlib>=3.6
openpyxl>=3.0
tqdm>=4.64
pyyaml>=6.0
# PyTorch — used by mt-cpso-f (pretrain + CPSO + finetune) and
# podtfm_baseline (POD-Transformer). Install a CUDA-compatible wheel
# matching your GPU/driver; see https://pytorch.org/get-started.
torch>=2.0
